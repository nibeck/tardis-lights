services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    privileged: true  # For GPIO access on rPi

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend